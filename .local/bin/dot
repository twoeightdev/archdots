#!/bin/sh
# Wrapper for dotfiles
dotfiles() { git --git-dir=$HOME/.config/.dots --work-tree=$HOME "$@"; }
if [ $1 = 'out' ]; then
    dotfiles pull &&
    dotfiles update-index --assume-unchanged ~/LICENSE ~/README.org &&
    rm -rf ~/LICENSE ~/README.org
elif [ $1 = 'in' ]; then
    dotfiles update-index --no-assume-unchanged ~/LICENSE ~/README.org &&
    dotfiles checkout -f
else
    dotfiles $*
fi
